#!/usr/bin/env bash
# Usage: manage reload
# Summary: Reload the server
# Help: This command is used to really reload the server process.
set -e
[[ -z $_MANAGE_ROOT ]] && exit 1

manage-mcrcon "reload"
